{
  "name": "goodshprd-stripe-checkout",
  "version": "1.0.0",
  "description": "Stripe Checkout integration for Good Shepherd website",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js",
    "lint": "echo 'Linting not configured yet'",
    "migrate": "node scripts/migrate.js"
  },
  "keywords": [
    "stripe",
    "payments",
    "checkout",
    "ecommerce"
  ],
  "author": "Good Shepherd",
  "license": "MIT",
  "dependencies": {
    "stripe": "^14.11.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "vercel": "^34.0.0",
    "@electric-sql/pglite": "^0.3.14"
  },
  "engines": {
    "node": "22.x"
  }
}
