/**
 * Deprecated: Find the Sheep module removed.
 * This file intentionally left minimal to avoid loading errors
 * if referenced by legacy HTML. You can safely remove the script tag
 * from any page that includes js/game.js.
 */

// No-op to avoid runtime errors if legacy handlers exist
document.addEventListener('DOMContentLoaded', function() {/* removed */});
